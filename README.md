# NONOGRAMS GAME
##### GROUP 05

To compile, run the following commands:

`javac -cp lib/javax.json-1.0.jar *.java`
`java -cp lib/javax.json-1.0.jar:. App`

This should open a puzzle selector for the puzzles in the Jsons/ directory
Select a puzzle and begin playing!